/**
 * @asseteragmbh/evm-contracts
 *
 * TypeScript SDK for the Assetera EVM contracts. Generated from the Foundry project in `../../contracts`
 * (ADR-0026): ABIs `as const`, per-chain addresses keyed by numeric chainId (with CAIP-2 in the body),
 * and a viem client factory.
 *
 * This is the package skeleton (Phase 1). The generated ABIs/addresses and the `createExchangeClient`
 * factory land in Phase 3.
 */
export * from "./contracts/index.js";
