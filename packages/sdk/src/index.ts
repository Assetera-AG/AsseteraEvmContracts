/**
 * @asseteragmbh/evm-contracts — the viem-first surface: ABIs `as const`, per-chain addresses, and a
 * contract-client factory. Generated from the Foundry contracts in `../../contracts` (ADR-0026).
 *
 * React apps: import the auto-address-resolving wagmi hooks from `@asseteragmbh/evm-contracts/react`.
 * Non-viem / non-TS consumers: import ABIs + addresses from `@asseteragmbh/evm-contracts/contracts`, or read
 * the raw `deployments/*.json` shipped in the package.
 */
export * from "./contracts/index.js";
export * from "./client.js";
