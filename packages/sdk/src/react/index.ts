/**
 * React entry (`@asseteragmbh/evm-contracts/react`) — the generated wagmi hooks. Read/write/watch hooks for
 * each contract, with the exchange proxy address auto-resolved from the connected chain (ADR-0026 D5).
 * Requires the `wagmi`, `@tanstack/react-query`, and `react` peer dependencies.
 */
export * from "../generated/react.js";
